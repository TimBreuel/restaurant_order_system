const connect = require("../models/connectionFn");
const ADDMENU = require("../models/menuSchema");

function addMeal(title, discription, number, price, category, img) {}

module.exports = { addMeal };
